# 🚀 Profile README — Deployment & Customization Guide

This package turns your GitHub profile into a premium, animated, mission-control
dashboard. Everything is **production-ready**. Follow the steps below.

---

## 📁 Folder Structure

```
Andy-XO/  (your profile repo)
├── README.md                       # The profile (renders on github.com/Andy-XO)
├── SETUP.md                        # This guide
├── assets/
│   ├── hero-banner.svg             # Custom animated hero (handcrafted SVG)
│   ├── footer-wave.svg             # Animated footer wave + tagline
│   └── stats.svg                   # Baked stats card (auto-generated, served from repo)
├── scripts/
│   └── gen_stats.py                # Builds stats.svg from live GitHub data
└── .github/
    └── workflows/
        ├── snake.yml               # Contribution snake animation → `output` branch
        └── stats.yml               # Refreshes stats.svg twice a day (no token needed)
```

---

## 🧭 The Key Concept: the special profile repo

Your README only shows on your profile if it lives in a repository **named exactly
the same as your username**:

```
Repository name:  Andy-XO/Andy-XO
```

If that repo doesn't exist yet, create it (it must be **Public**, and GitHub will
offer a "you found a secret" hint when the name matches your username).

---

## ⚡ Quick Deploy (3 commands)

From inside the `github-profile/` folder:

```bash
# 1. Initialize and point at YOUR profile repo
git init
git add .
git commit -m "feat: premium animated profile README"
git branch -M main

# 2. Connect to the special repo (create Andy-XO/Andy-XO on github.com first)
git remote add origin https://github.com/Andy-XO/Andy-XO.git

# 3. Push
git push -u origin main
```

Open `https://github.com/Andy-XO` — the profile is live. ✨

> **Note:** the contribution snake image shows as broken for a few minutes
> until its Action runs for the first time. That's expected — see below.

---

## 🐍 Enable the Snake Animation

The snake SVG is generated by a GitHub Action — it isn't a static file.

1. After pushing, go to the repo's **Actions** tab → enable workflows if prompted.
2. Open **"Generate Snake Animation"** → **Run workflow** (manual first run).
3. It creates an `output` branch containing `github-snake-dark.svg`.
4. The README already points at:
   `https://raw.githubusercontent.com/Andy-XO/Andy-XO/output/github-snake-dark.svg`

After that it auto-refreshes every 12 hours. No token required.

---

## 📊 Stats Card (no setup needed)

The Analytics section uses a **baked stats card** (`assets/stats.svg`) instead of
the public stat services that frequently rate-limit (503 / 402). It's a real SVG
committed to your repo, so GitHub serves it directly and it can never break.

- **Generator:** `scripts/gen_stats.py` pulls your public stats (followers, repos,
  stars, forks, top languages) from the GitHub API and renders the cyan-dark card.
- **Auto-refresh:** `.github/workflows/stats.yml` regenerates it twice a day using
  the built-in `GITHUB_TOKEN` — **no personal token or secret required.**
- **Regenerate on demand:** Actions tab → **"Refresh Stats Card"** → Run workflow.
- **Change what it shows:** edit the theme colors or layout in `gen_stats.py`.

> Want more repos/languages reflected? The card counts your **public** repos.
> Make a private repo public and the next refresh picks it up automatically.

Skip this entirely if you don't want it — the rest of the profile is unaffected.

---

## ✏️ Personalize — search for `▷ EDIT`

Open `README.md` and update these (all are marked with `▷ EDIT` comments):

| What | Where | Current placeholder |
|------|-------|---------------------|
| LinkedIn URL | Social row + Contact section | `linkedin.com/in/atharva-patil` |
| X / Twitter handle | Social row + Contact section | `x.com/AtharvaPatil` |
| Email | already correct | `atharvapatil503@gmail.com` |

Find/replace tips:
- Replace **`atharva-patil`** with your real LinkedIn slug.
- Replace **`AtharvaPatil`** with your real X handle.

---

## 🎨 Customization Guide

### Change the accent color
The theme is built on three cyan/blue stops. To re-skin globally, find/replace
these hex codes across `README.md`, `assets/*.svg`, and `.github/workflows/snake.yml`:

| Role | Hex |
|------|-----|
| Primary (cyan) | `00D9FF` |
| Secondary (blue) | `33A1FF` |
| Tertiary (indigo) | `6E7BFF` |
| Background | `05070D` |
| Muted text | `9FB4C7` |

Example: for a violet theme, swap `00D9FF → A78BFA`, `33A1FF → 8B5CF6`, `6E7BFF → 6366F1`.

### Edit the typing animation
In README.md, the `readme-typing-svg` URL has a `lines=` query param — change the
text segments (use `;` to separate lines, `%C2%B7` is the `·` separator).

### Edit the hero / footer banners
`assets/hero-banner.svg` and `assets/footer-wave.svg` are plain, commented SVG.
Change the `<text>` content, the gradient `<stop>` colors, or animation `dur=`
values directly. They animate via SMIL, which GitHub renders natively.

### Reorder / remove sections
Each section is fenced with a banner comment like:
```
<!-- SECTION · FEATURED PROJECTS -->
```
Cut/paste whole blocks to reorder. The `01 ⟶`, `02 ⟶` numbering is just text —
renumber as you like.

### Add a real project link
In the Featured Projects table, wrap a card title in a link:
```markdown
### [🛰 VoltAeroTech](https://github.com/Andy-XO/voltaerotech)
```

---

## 🧩 What's Used (all free, no backend)

| Feature | Service |
|---------|---------|
| Hero & footer banners | Handcrafted animated SVG (this repo) |
| **Stats card** | **Baked SVG from `scripts/gen_stats.py` (this repo) — can't rate-limit** |
| Typing animation | `readme-typing-svg.demolab.com` |
| Tech badges | `img.shields.io` |
| Streak | `github-readme-streak-stats.herokuapp.com` |
| Activity graph | `github-readme-activity-graph.vercel.app` |
| Snake | `Platane/snk` GitHub Action |
| Visitor counter | `komarev.com/ghpvc` |

---

## ✅ Post-deploy checklist

- [ ] Repo `Andy-XO/Andy-XO` exists and is **Public**
- [ ] `README.md` + `assets/` + `.github/` pushed to `main`
- [ ] Snake workflow run once → `output` branch exists
- [ ] LinkedIn + X placeholders replaced with real URLs
- [ ] Stats card (`assets/stats.svg`) present and rendering
- [ ] Profile looks right on desktop **and** mobile

---

Built to look like the profile of an engineer shipping intelligent systems into
the real world. Push it and own it. 🛰
